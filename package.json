{
  "name": "auto-strings-generator",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/extension.js",
  "scripts": {
    "build": "tsc --noEmit && tsx build.ts",
    "package": "npm run build && extensions-cli package . -o dist/auto-strings-generator.ablx",
    "prepare:run": "npm run build && node scripts/prepareRun.mjs",
    "run:live": "npm run prepare:run && extensions-cli run .ableton-run --live \"/Applications/Ableton Live 12 Beta.app/Contents/Helpers/ExtensionHost/ExtensionHostNodeModule.node\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@ableton-extensions/sdk": "file:vendor/ableton-extensions-sdk-1.0.0-beta.0.tgz"
  },
  "devDependencies": {
    "@ableton-extensions/cli": "file:vendor/ableton-extensions-cli-1.0.0-beta.0.tgz",
    "@types/node": "^25.9.1",
    "esbuild": "0.28.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.5"
  }
}
