# Vendor SDK Packages

Place Ableton's official Live Extensions SDK beta tarballs here before running `npm install`.

Expected filenames:

- `ableton-extensions-sdk-1.0.0-beta.0.tgz`
- `ableton-extensions-cli-1.0.0-beta.0.tgz`

The tarballs are intentionally not committed to this public repository.
